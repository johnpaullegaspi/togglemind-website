{
  "name": "togglemind-ai-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing website for Toggle Mind AI. Plain HTML5/CSS3/vanilla JS, with a small zero-dependency build script that renders content from _content/**/*.json and _data/settings.json (managed via Decap CMS / DecapBridge) into index.html.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
